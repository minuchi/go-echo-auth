# go-echo-auth

Login sever with echo, gorm and postgresql.

## APIs
* `/api/auth/v1`
  * `GET` `/time` 
  * `POST` `/login`
  * `POST` `/signup`
  * `POST` `/token`
  * `POST` `/verify` **Authorized**
